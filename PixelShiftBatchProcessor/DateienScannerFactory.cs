using PixelShiftBatchProcessor.NewFolder;

namespace PixelShiftBatchProcessor
{
    internal class DateienScannerFactory
    {
        //public static IDateienScanner CreateDateienScanner(string filePath)
        //{
        //    if (filePath.EndsWith(".arw", StringComparison.OrdinalIgnoreCase))
        //    {
        //        return new ArwDateienScanner(filePath);
        //    }
        //    else if (filePath.EndsWith(".tiff", StringComparison.OrdinalIgnoreCase) || filePath.EndsWith(".tif", StringComparison.OrdinalIgnoreCase))
        //    {
        //        return new TiffDateienScanner(filePath);
        //    }
        //    else
        //    {
        //        throw new NotSupportedException($"Dateiformat für {filePath} wird nicht unterstützt.");
        //    }
        //}
    }
}
